# The Cloudflare Pages workflow runs `pages deploy .` over a git checkout, so
# anything committed here is published at polkaspots.com. Internal strategy docs
# are kept locally for reference and must never be committed.
docs/

# Python bytecode from tools/
__pycache__/
*.pyc
